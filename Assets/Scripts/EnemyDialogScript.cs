using UnityEngine;

public class EnemyDialogScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Hej jeg er ond");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
